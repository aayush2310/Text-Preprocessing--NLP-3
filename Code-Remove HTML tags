import re
def remove_html_tags(text):
   pattern=re.compile('<.*?>')
   return pattern.sub(r'', text)
   
text="<html><body><p>ghch</p><p>hv jtgc hgchf hch</p></body></html>"

(text)

text

df['review']=df['review'].apply(remove_html_tags)

df['review'][3]
